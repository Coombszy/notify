# Sample docker-compose file 
version: '3.3'

services:
  notify:
    container_name: notify
    image: notify:latest
    volumes:
      - ./testing/config:/app/config
      - ./testing/notifications:/app/notifications
